# remap prefix
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Mouse mode
set -g mouse on

# Start screen numbering from 1
set -g base-index 1
setw -g pane-base-index 1

# Set repeat time longer so that holding buttons work
set -g repeat-time 750
